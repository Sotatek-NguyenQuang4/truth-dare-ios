# Uncomment the next line to define a global platform for your project

target 'truth-or-date-ios' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for truth-or-date-ios
  pod 'SnapKit', '~> 5.0.1'
  pod 'lottie-ios'
  pod 'SwiftFortuneWheel'

  # Service
  pod 'Alamofire', '~> 5.4'
  pod 'AlamofireImage', '~> 4.1'
  
  pod 'SwiftyStoreKit'

  target 'truth-or-date-iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'truth-or-date-iosUITests' do
    # Pods for testing
  end

end
